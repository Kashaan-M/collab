const expect = require('chai').expect;
const { signup } = require('../controllers/signup');

describe('Sign up Controller', () => {
  it('displayName is provided', () => {
    const req = {};
  });
});
